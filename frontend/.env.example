# Base URL of the deployed backend API, WITHOUT a trailing slash.
# In local dev this is not needed -- Vite proxies /api to localhost:8000
# (see vite.config.js). Set this only for production builds where the
# frontend and backend are deployed as separate services.
VITE_API_BASE_URL=https://your-backend-host.onrender.com
